server:
  port: 8888

spring:
  application:
    name: github-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/RuslanSakaev/config.git
          basedir: file://c:/tmp/shop
          default-label: main
